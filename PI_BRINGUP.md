# Приведення заліза RB → 21 датчик (робочий зошит)

Хендовер для роботи з **ноута (Linux) через Claude Code**, який по SSH керує
Raspberry. Тут — мета, парк датчиків, робочий цикл і типові проблеми. RB
тримаємо **лише в локальній мережі** (Ethernet/Wi-Fi); в інтернет не виставляємо.

## Поточний стан (передача з хмарної сесії)

Софт повністю готовий і запушений (гілка `claude/clever-keller-f6h8h2`). Кіоск,
автозапуск, мультисенсорна логіка, тести — усе на місці.

Обрано **варіант B (мукс на кожен стелаж)**, розкладка вже у `config.farm.json`
(`мукс = 0x70 + стелаж−1`, `канал = поверх−1`). На RB активувати:
`cp config.farm.json config.json` і перезапустити кіоск.

**Залізо:** RB (hostname `rnd`, user `termo-sys`) з 7″ екраном, кіоск-автозапуск
працює. Спаяно **перший мукс** (стелаж 1, адреса 0x70, страпи A0/A1/A2→GND).
Датчик ще НЕ підключено — треба спаяти один HDC1080 у канал **SD1/SC1**
(середня полиця = поверх 2; верхня й нижня порожні).

**ГОЛОВНИЙ БЛОКЕР — мережа.** Ноут (Fedora, дротовий інтерфейс `enp98s0`,
`192.168.1.2/24`) не бачить RB. Причина: пряме з'єднання ноут↔RB кабелем **без
DHCP**, тож RB не отримує сумісний IP. Wi-Fi на RB, схоже, не налаштований.
Рішення — хай ноут роздає IP на дротовий інтерфейс:
`sudo nmcli con modify "<wired-conn>" ipv4.method shared && sudo nmcli con up
"<wired-conn>"`, зачекати ~1 хв, тоді `sudo nmap -sn 10.42.0.0/24` і
`ssh termo-sys@10.42.0.XX`. (Локальний Claude Code, на відміну від хмарної
сесії, має прямий доступ до цього кабелю — тож може реально це полагодити.)

**Наступні кроки по порядку:**
1. Підняти зв'язок ноут↔RB (роздача IP вище) → `ssh` на RB.
2. `cd ~/farm-ui && git pull && i2cdetect -y 1` → має бути `70`.
3. Спаяти датчик у `SD1/SC1`, `i2c_scan.py` → `ch1: 0x40(HDC1080)`.
4. `cp config.farm.json config.json`, перезапуск кіоску → «Стелаж 1 · Середній»
   оживає.
5. Далі тиражувати: стелаж 2 → мукс 0x71, стелаж 3 → 0x72 (є 3 мукси; ще 4
   докупити для повних 21).

## Мета

Довести термо-контроль ферми до кінця: підключити й перевірити **21 датчик**
(7 стелажів × 3 поверхи) на реальному залізі, від 1 до 21, без правок коду.

## Що вже готове (софт)

- `farm_monitor.py` — UI-кіоск + `HardwareDataSource` з **автовизначенням** типу
  датчика в кожному каналі TCA9548A (`0x38` AHT10/AHT20, `0x40` HDC1080,
  `0x76/0x77` BMP280/BME280 — тиск). Офлайн-канали перевизначаються; датчик/мукс,
  підключені пізніше, підхоплюються **без перезапуску**.
- `farm_config.py` + `config.json` — розкладка, пороги, проводка, калібрування.
- `check_config.py` — показати підсумкову карту датчиків (без Qt/заліза).
- `i2c_scan.py` — що на яких каналах.
- `deploy_to_pi.sh` (з ноута) / `diag.sh` (на RB) — швидкий цикл.
- Тести: `python3 -m unittest discover -s tests` (17 шт).

## Парк датчиків

| Тип | К-сть | I2C | Дає |
|-----|------:|-----|-----|
| HDC1080 | 19 | 0x40 | T + RH |
| AHT10 | 5 | 0x38 | T + RH |
| AHT20 + BMP280 | 2 | 0x38 (+0x76/0x77) | T + RH (+ тиск) |
| HW-617 (BMP280) | 4 | 0x76/0x77 | тиск + T |

21 позиція — переважно HDC1080 + кілька AHT; BMP280 там, де потрібен тиск.
Запас датчиків є. **Один T/RH-датчик на канал** (адреси в межах каналу не
конфліктують — мукс ізолює канали).

## Схема підключення (за замовчуванням)

- Один TCA9548A на поверх: **поверх 1 → 0x70, 2 → 0x71, 3 → 0x72**.
- **Канал = номер стелажа − 1** (стелаж 1..7 → канал 0..6).
- Адреса мукса задається піном: `A0/A1/A2 → GND` = 0x70; `A0 → 3V3` = 0x71;
  `A1 → 3V3` = 0x72.
- Pi `3V3`→TCA `VIN`, `GND`→`GND`, `SDA`(pin3)→`SDA`, `SCL`(pin5)→`SCL`.
- Канал N: `SDN/SCN` → датчик `SDA/SCL`; датчик `VIN`→3V3, `GND`→GND.

Нестандартна проводка? Не чіпай код — задай у `config.json → sensors[]`
`{"rack":R,"floor":F,"mux":"0x71","channel":C,"type":"auto"}`, перевір
`python3 check_config.py`.

## Робочий цикл з ноута

```bash
# одноразово: SSH-ключ на RB (без пароля)
ssh-copy-id pi@raspberrypi.local

# залив коду на RB
PI_HOST=pi@raspberrypi.local bash deploy_to_pi.sh

# залив + встановлення залежностей (перший раз)
RUN=setup PI_HOST=pi@raspberrypi.local bash deploy_to_pi.sh

# залив + діагностика (i2cdetect + i2c_scan + check_config)
RUN=diag PI_HOST=pi@raspberrypi.local bash deploy_to_pi.sh

# залив + запуск на реальних датчиках
RUN=hardware PI_HOST=pi@raspberrypi.local bash deploy_to_pi.sh
```

`config.json`, `alerts.log`, `*.csv`, venvʼи на RB rsync **не чіпає** (вони в
excludes) — локальні налаштування Pi зберігаються.

## Порядок приведення 21 датчика

1. `RUN=setup` один раз (I2C + бібліотеки; якщо I2C щойно увімкнено — `sudo reboot`).
2. Підключай мукси й датчики поступово.
3. Після кожної партії — `RUN=diag`: у `i2c_scan.py` мають зʼявлятись
   `0x38/0x40/0x76` на потрібних каналах; `check_config.py` — карта позицій.
4. `RUN=hardware` — у кіоску підключені датчики зелені/жовті/червоні, решта сірі
   (offline); чип «датчики» = `N/21`. Ціль — `21/21`.

## Калібрування

Порівняй показ із довіреним приладом і впиши зсуви в `config.json`:
```json
"calibration": { "4,2": { "t_offset": -0.6, "h_offset": 2.0 } }
```
Ключ — `"стелаж,поверх"`. HDC1080 і AHT можуть трохи різнитись — зсуви
вирівнюють парк.

## Типові проблеми

- **Немає 0x70 у `i2cdetect`** → I2C вимкнено або проводка SDA/SCL/живлення.
  `sudo raspi-config nonint do_i2c 0; sudo reboot`.
- **Мукс є, каналів порожньо** → живлення датчика (3V3/GND) або SD*/SC* переплутані.
- **Датчик на несподіваній адресі** → скажи Claude: підправимо автовизначення
  або задамо `type` у `config.json`.
- **`i2c_scan` падає на import board** → не з `.venv-hw` запущено; став
  `.venv-hw/bin/python`.
- **Кіоск не стартує після зламаного `config.json`** → застосунок сам падає на
  дефолти й пише причину в термінал (`[config] ...`).

## Керування з ноута через Claude Code

Локальний Claude Code — окремий сеанс (нашої переписки він не бачить), але цей
файл + репозиторій дають йому весь контекст. Запусти Claude Code в теці репо на
ноуті й попроси: «приведи 21 датчик за `PI_BRINGUP.md`, RB на
`pi@raspberrypi.local`». Далі він сам ганятиме `deploy_to_pi.sh`/`diag.sh` по SSH
і правитиме конфіг/код наживо.
